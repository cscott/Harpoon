/* On IRIX build systems (and possibly others?) automake/libtool has
 * problems with libraries which don't contain any source files.  So
 * this is just a trivial source file to include in otherwise-empty
 * libraries. --CSA 2003-10-06 */

static int a_pointless_symbol=0;
