<!-- $Id: configure.xml,v 1.1 2002-07-02 15:34:35 wbeebee Exp $ -->

<!--+
    |
    | This file contains the jTools XML configuration script. jTools uses ANT
    | as building tool, and at configuration time, all a bounch of useful
    | properties are defined. The property being defined at configuration
    | time are mostly used to keep all the build file clear and self-
    | documenting.
    |
    |
    | File: configure.xml
    | Author: Angelo Corsaro <corsaro@doc.ece.uci.edu>
    | $Revision: 1.1 $
    |
    |
    | NOTE: Some part of this file is "mis-indented" on purpose
    |       the reason is to have the config file to be generated
    |       with the proper indentation.
    +-->

<project name="Config" default="all" basedir=".">

    <property name="config-file" value="jtools-build-bootstrap.properties"/>
    <target name="all" depends="header, body, trailer"/>

    <target name="header">
       <echo file="${config-file}">
###########################################################
#   DO NOT CHANGE MANUALLY THE CONTENTS OF THIS FILE      #
#   THIS FILE WAS AUTOMATICALLY GENERATED BY THE jTools   #
#   CONFIGURATION SCRIPT                                  #
###########################################################
      </echo>
    </target>

    <path id="jtools.os.base">
       <pathelement location="${basedir}"/>
    </path>

    <target name="body">
       <pathconvert targetos="unix" property="new.jtools.basedir" refid="jtools.os.base"/>
       <echo  file="${config-file}" append="yes">
#
# Set the property needed to build ${PROJECT-NAME}
#
jtools.basedir=${new.jtools.basedir}
jtools.srcdir = ${new.jtools.basedir}/src
jtools.bindir = ${new.jtools.basedir}/bin
jtools.demodir = ${new.jtools.basedir}/demo
jtools.testdir = ${new.jtools.basedir}/src/edu/uci/ece/jtools/test
jtools.gendir = ${new.jtools.basedir}/src/generated
jtools.classdir = ${new.jtools.basedir}/classes
jtools.libdir = ${new.jtools.basedir}/lib
jtools.classpath = ${jtools.classdir}:${java.class.path}

#
# Module properties
#
jtools.modules.basedir = ${new.jtools.basedir}/src/modules

#
# Some Handy Settings for Emacs/XEmacs Users
#
build.compiler.emacs = true

#
# Set Paranoia-Level warnings!!!
#
build.compiler.pedantic = true

       </echo>
    </target>

   <property name="args" value="$"/>


   <target name="trailer">
      <mkdir dir="${new.jtools.basedir}/classes"/>
      <mkdir dir="${new.jtools.basedir}/lib"/>
   </target>

</project>
