<!-- $Id: configure.xml,v 1.1 2002-07-02 15:34:19 wbeebee Exp $ -->

<!--+
    |
    | This file contains the RTJPerf XML configuration script. RTJPerf uses ANT
    | as building tool, and at configuration time, all a bounch of useful
    | properties are defined. The property being defined at configuration
    | time are mostly used to keep all the build file clear and self-
    | documenting.
    |
    |
    | File: configure.xml
    | Author: Angelo Corsaro <corsaro@doc.ece.uci.edu>
    | $Revision: 1.1 $
    |
    |
    | NOTE: Some part of this file is "mis-indented" on purpose
    |       the reason is to have the config file to be generated
    |       with the proper indentation.
    +-->

<project name="Config" default="all" basedir=".">

    <property name="config-file" value="rtjperf-build-bootstrap.properties"/>
    <target name="all" depends="header, body, trailer"/>

    <target name="header">
       <echo file="${config-file}">
###########################################################
#   DO NOT CHANGE MANUALLY THE CONTENTS OF THIS FILE      #
#   THIS FILE WAS AUTOMATICALLY GENERATED BY THE RTJPerf  #
#   CONFIGURATION SCRIPT                                  #
##########################################################
      </echo>
    </target>

    <path id="rtjperf.os.base">
       <pathelement location="${basedir}"/>
    </path>

    <target name="body">
       <pathconvert targetos="unix" property="new.rtjperf.basedir" refid="rtjperf.os.base"/>
       <echo  file="${config-file}" append="yes">
#
# Set the property needed to build ${PROJECT-NAME}
#
rtjperf.basedir=${new.rtjperf.basedir}
rtjperf.srcdir = ${new.rtjperf.basedir}/src
rtjperf.bindir = ${new.rtjperf.basedir}/bin
rtjperf.demodir = ${new.rtjperf.basedir}/demo
rtjperf.testdir = ${new.rtjperf.basedir}/src/edu/uci/ece/rtjperf/test
rtjperf.gendir = ${new.rtjperf.basedir}/src/generated
rtjperf.classdir = ${new.rtjperf.basedir}/classes
rtjperf.libdir = ${new.rtjperf.basedir}/lib
rtjperf.classpath = ${rtjperf.classdir}:${java.class.path}

#
# Module properties
#
rtjperf.modules.basedir = ${new.rtjperf.basedir}/src/modules

#
# Some Handy Settings for Emacs/XEmacs Users
#
build.compiler.emacs = true

#
# Set Paranoia-Level warnings!!!
#
build.compiler.pedantic = true

       </echo>
    </target>

   <property name="args" value="$"/>


   <target name="trailer">
      <mkdir dir="${new.rtjperf.basedir}/classes"/>
      <mkdir dir="${new.rtjperf.basedir}/lib"/>
   </target>

</project>
