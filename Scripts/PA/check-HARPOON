# make sure HARPOON_DIR is initialized
if [ ! -n "${HARPOON_DIR}" -a -d ${HARPOON_DIR} ]
then
    echo "Please set HARPOON_DIR to the Harpoon root directory";
    echo "e.g. export HARPOON_DIR=/home/salcianu/Harpoon";
    exit 1;
fi
